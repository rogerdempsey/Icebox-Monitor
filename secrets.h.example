// Template for secrets.h - copy this file to secrets.h and fill in your own
// Wi-Fi credentials. secrets.h itself is gitignored and never committed;
// this .example file is the only one that should be checked into the repo.
//
//   cp secrets.h.example secrets.h
//
// then edit secrets.h with your real Wi-Fi credentials.
#pragma once

const char* WIFI_SSID     = "YOUR_WIFI_SSID";
const char* WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";
